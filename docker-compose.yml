version: '3'
services:
  integration_node_express:
    build:
      context: .
      dockerfile: ./__tests__/integrations/node.Dockerfile
    command: npm run test:integration
    environment:
      - EXAMPLE_SERVER=./packages/node/examples/express/index.js
