/* eslint-env mocha */
describe('processRequest()', () => {
  it('should strip blacklisted properties');
  it('should only send whitelisted properties');
  it('should error if blacklist AND whitelist provided');
});
